package BasicsOfJava;

public class array {
    public static void main(String[] args) {
        int arr[] = { 1, 2, 7, 3, 4, 10, 8,  };
        // int x = 9;
        // for(int i=arr.length-1;i>=0;i--){//print reverse of array
        // System.out.println(arr[i]);
        // }

        // int sum=0;
        // int avg=0;
        // for(int i=0;i<arr.length;i++){//sum and average of array element
        // sum=sum+arr[i];
        // avg=sum/arr.length;
        // }
        // System.out.println(sum);
        // System.out.println(avg);

        // int max=arr[0];
        // for(int i=0;i<arr.length;i++){//maximum number find karna
        // if(arr[i]>max){
        // max=arr[i];
        // }
        // }
        // System.out.println("maximam value is"+ max);

        // int max = arr[0];
        // int secondmax = Integer.MIN_VALUE;
        // for (int i = 0; i < arr.length; i++) {// second maximum number find karna
        // if (arr[i] > max) {
        // secondmax = max;
        // max = arr[i];
        // }
        // else if (arr[i] > secondmax && arr[i] != max) {
        // secondmax = arr[i];
        // }
        // }
        // System.out.println("maximam value is:" + max);
        // System.out.println("second maximum value is:" + secondmax);

        // for (int i = 0; i < arr.length; i++) {// print index of sum of two elements
        //     for (int j = i + 1; j < arr.length; j++) {
        //         if (arr[i] + arr[j] == x) {
        //             System.out.println("indices are:" + i + " " + j);
        //         }
        //     }
        // }
    }
}
